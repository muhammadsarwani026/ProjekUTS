<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muhammadsarwani.projekuts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wallet1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TambahTransaksi"
            android:label="Tambah Transaksi"/>
        <activity android:name=".UpdateTransaksi"
            android:label="Update Transaksi"/>
        <activity android:name=".DeleteTransaksi"
            android:label="Delete Transaksi"></activity>
    </application>

</manifest>